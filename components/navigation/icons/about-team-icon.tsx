import { Users } from "lucide-react";

import type { NavMenuIconProps } from "@/components/navigation/icons/nav-menu-icon-props";

export function AboutTeamIcon(props: NavMenuIconProps) {
  return <Users strokeWidth={1.75} {...props} />;
}
